# Minecraft server's RCON settings (must match server.properties)
RCON_HOST=localhost
RCON_PORT=25575
RCON_PASSWORD=your_rcon_password_here

# Shared secret between agent and bot — generate with: python -c "import secrets; print(secrets.token_hex(32))"
AGENT_API_KEY=replace_with_random_hex_key
